{
  "name": "mytiktokanalytics-oauth-proxy",
  "version": "1.0.0",
  "description": "OAuth proxy for MyTikTokAnalytics GitHub Pages integration",
  "main": "index.js",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'No build process needed for Netlify Functions'",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "node-fetch": "^2.6.7"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
